# kadaciagogo_game 操作說明

本專案為 **「2026 我們的騎跡」Day 3 大地遊戲** 網頁應用：Next.js + React，以瀏覽器完成景點任務、同步積分至伺服器 SQLite，並提供排行榜與圖片拼貼功能。

---

## 一、環境需求

- **Node.js**：建議使用目前的 LTS 版本（與 Next.js 16 相容）。
- **npm**（或 pnpm / yarn / bun，以下以 npm 為例）。
- **better-sqlite3** 為原生模組：若 `npm install` 失敗，請安裝對應平台的建置工具（Windows 上常見為 Visual Studio Build Tools 與「使用 C++ 的桌面開發」工作負載）。

---

## 二、安裝與啟動

在專案根目錄執行：

```bash
npm install
```

### 開發模式

```bash
npm run dev
```

瀏覽器開啟 **http://localhost:3000** 即可使用。修改程式碼後會熱更新。

### 正式建置與執行

```bash
npm run build
npm start
```

預設仍為 **http://localhost:3000**（除非透過環境變數指定埠號）。

### 程式碼檢查

```bash
npm run lint
```

---

## 三、使用者端操作（參與者）

### 1. 首次進入

- 首次會播放開場動畫；完成後可進入主畫面。
- 若曾看過開場，瀏覽器會記住（`localStorage`），下次可略過。

### 2. 註冊真實姓名

- 必須輸入 **2～3 個字中文姓名**（繁簡漢字），不可為匿名、測試等禁用字樣。
- 新用戶會取得 **用戶代碼**（10 碼），並寫入本機與伺服器。
- 若舊資料僅有用戶代碼而無姓名，系統會要求 **補登姓名**。

### 3. 三個分頁

| 分頁 | 說明 |
|------|------|
| **任務** | 列出所有景點；點選進入劇情與任務。 |
| **排行榜** | 依總積分排序；同分則依最後打卡時間；時間為 **台北時間（UTC+8）**。 |
| **圖片拼貼** | 需先完成 **至少 6 個「一般景點」** 的拍照任務（**終極任務、特級任務不計入**），解鎖後可將已拍照片製成拼貼。 |

頁首會顯示一般景點完成進度（例如 `一般景點 6 / N`）。

### 4. 單一景點任務流程

1. **劇情模式**：景點介紹文字逐字顯示，播完後點畫面空白處進入任務。
2. **一般／終極（拍照）**：閱讀完畢後可 **拍照或從相簿選圖**；成功後會壓縮圖片並存於本機，並向伺服器 **打卡** 取得積分。
3. **特級任務（心得）**：需撰寫心得，**有效字數不含標點**（空白不計），達門檻後 **提交心得並取得積分**；提交後心得內容會同步至伺服器。

### 5. 資料儲存位置（參與者須知）

- **本機**：進度與照片縮圖存在瀏覽器 `localStorage`（鍵名見程式 `kadaciagogo_day3_2026`）。清除網站資料會遺失本機進度，請謹慎操作。
- **伺服器**：註冊、打卡、特級心得會寫入專案目錄下的 SQLite（見下一節）。

---

## 四、伺服器資料與維運

### 資料庫檔案

- 路徑：**`data/game.db`**
- 首次有 API 寫入時會自動建立 `data` 目錄與資料表。
- 備份：直接複製 **`game.db`** 即可；建議在無寫入或短暫停機時備份以保一致。

### 主要資料表（概念）

- **users**：用戶代碼、真實姓名、註冊時間等。
- **checkins**：各景點打卡、積分、時間（同一用戶同一景點僅一筆，重複提交會標記為 duplicate，不重複加分）。
- **reflections**：特級任務心得全文（可更新同一景點的心得）。

---

## 五、活動後報表（命令列）

專案提供 **`scripts/sqlite-stats.mjs`**，以唯讀方式讀取 `data/game.db`，適合活動結束後匯總。

在專案根目錄執行（亦可使用 package.json 中的捷徑）：

| 指令 | 說明 |
|------|------|
| `npm run stats` | 等同無參數：輸出 **全域摘要** + **排行榜（預設前 30 名）**。 |
| `npm run stats:users` | 列出所有用戶（代碼、姓名、註冊時間）。 |
| `npm run stats:reflections` | 列出所有特級任務心得全文。 |
| `node scripts/sqlite-stats.mjs --global` | 僅全域統計。 |
| `node scripts/sqlite-stats.mjs --leaderboard 20` | 僅排行榜前 20 名。 |
| `node scripts/sqlite-stats.mjs --user <用戶代碼>` | 單一用戶打卡明細、心得與總分。 |
| `node scripts/sqlite-stats.mjs --help` | 顯示完整說明。 |

若出現「找不到資料庫」，請先在本機執行過網站並完成至少一次會寫入 DB 的操作，或確認 **`data/game.db`** 是否存在。

---

## 六、景點與靜態資料

- 景點清單由 **`public/data/spots.json`** 提供；修改檔案後重新整理頁面即可反映（開發模式下無需重啟伺服器）。
- 景點圖片路徑等請與 `spots.json` 內欄位一致。

---

## 七、API 一覽（供維運／除錯）

| 路徑 | 方法 | 用途 |
|------|------|------|
| `/api/register` | POST | 新用戶註冊（JSON：`realName`） |
| `/api/profile` | POST | 補登／更新真實姓名（JSON：`userCode`, `realName`） |
| `/api/checkin` | POST | 打卡與選填心得（JSON：`userCode`, `spotId`, `points`, 選填 `reflectionText`） |
| `/api/leaderboard` | GET | 排行榜（查詢參數如 `limit`） |

---

## 八、常見問題

**Q：排行榜沒有我的分數？**  
請確認已完成註冊、且該景點已成功打卡（網路錯誤時可檢查瀏覽器開發者工具）。特級任務需送出心得並達字數門檻。

**Q：換手機或清除 Cookie 後進度不見？**  
本機進度存在瀏覽器；伺服器僅保存積分與心得等伺服器端資料，無法還原本機照片，請以活動說明為準。

**Q：正式環境要架在哪？**  
需可長期執行 Node 程序、可寫入 **`data/game.db`** 的磁碟，並自行設定 HTTPS、備份與防火牆。若使用無檔案系統或唯讀檔案系統的 Serverless，需另行設計資料庫儲存，與目前程式不相容。

---

以上說明對應專案根目錄之 `package.json` 指令與現行程式行為；若程式更新，請以實際程式碼為準。
